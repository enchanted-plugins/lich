{
  "description": "mantis-rubric hooks — advisory-only. PostToolUse chain (after mantis-core + mantis-sandbox) triggers M7 judgment. See @shared/conduct/hooks.md.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/dispatch.sh\" mantis-judge",
            "timeout": 15000
          }
        ]
      }
    ]
  }
}
