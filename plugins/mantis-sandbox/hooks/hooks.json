{
  "description": "mantis-sandbox hooks — advisory-only, never blocking. Chained after mantis-core PostToolUse fires (bash dispatch.sh reads review-flags.jsonl and triggers this sub-plugin). See @shared/conduct/hooks.md.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/dispatch.sh\" mantis-sandbox",
            "timeout": 30000
          }
        ]
      }
    ]
  }
}
